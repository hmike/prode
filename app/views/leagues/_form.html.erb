<%= form_for @league, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :league_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :league_type, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :groups_count, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :groups_count, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :teams_count, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :teams_count, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                leagues_path, :class => 'btn' %>
  </div>
<% end %>
